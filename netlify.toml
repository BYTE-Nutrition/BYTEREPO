# Marketing site: static HTML in website/ — no build step, no Supabase needed.
# The React app lives in app/ and can be hosted separately (e.g. a second Netlify site).
[build]
  publish = "website"

[build.environment]
  NODE_VERSION = "22"
